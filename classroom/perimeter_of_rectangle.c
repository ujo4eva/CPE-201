#include <stdio.h>

int main() {
   
    int length = 20;
    int height = 9;

    int perimeter = 2*(20+9);

    printf("The Perimeter of a rectangle given length %d and height %d is %d.\n", length, height, perimeter);

    return 0;
}